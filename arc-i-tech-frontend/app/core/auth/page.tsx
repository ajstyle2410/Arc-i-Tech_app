export default function LoginPage() {
  return (
    <div className="container text-center mt-5">
      <h2 className="fw-bold">Login</h2>
      <p>Protected pages redirect here if user is not logged in.</p>
    </div>
  );
}
